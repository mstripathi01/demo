import logo from "./logo.svg";
import "./App.css";

function App() {
  return (
    <div className="App">
      <h4>
        Both are super useful for working with objects when you need to process
        either the keys or values separately! 😊
      </h4>
      <p>Hello this is mayank</p>
    </div>
  );
}

export default App;
